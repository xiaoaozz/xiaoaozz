# profile-snake-contrib

此目录由 `.github/workflows/snake.yml` 自动生成。

首次 push 后，请在 GitHub 仓库的 **Actions** 页面手动运行一次 `Generate Snake` workflow，
生成完成后这里会出现 `github-contribution-grid-snake.svg`（含 dark/light 两个版本），
之后 workflow 会按 cron 计划自动更新。
